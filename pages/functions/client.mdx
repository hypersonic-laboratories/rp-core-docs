# Functions
This is a class used to manage and maintain multiple different points. Allowing tracking players interactions easier.


### GetConfig()
Sets the marker as visible.

```lua copy
local cfg = Core.GetConfig()
```

### Core.RegisterHUD(...args)
name, location, closeEvent
```lua copy
local hud = Core.RegisterHUD('hud', './ui/main.html', 'closeEvent')

Events.Subscribe('closeEvent', function()
    -- Ran when the hud is closed
end)
```

### Core.GetHUD(name)
```lua copy
local hud = Core.GetHUD('hud')

-- Logic here
```

### Core.PlayAnimation(...args)
anim, slot_flag, loop, blend_in_time, blend_out_time, play_rate, stop_all_montages
```lua copy
Core.PlayAnimation("griddy", AnimationSlotType.FullBody, false, 0.25, 0.25, 1.0, false)
```

### Core.StopAnimation(anim)
```lua copy
Core.StopAnimation("griddy")
```

### Core.RegisterInput(...args)
key, onPress, onRelease
```lua copy
local onPress = function()

end

local onRelease = function()

end

Core.RegisterInput("E", onPress, onRelease)
```

### Core.HideHUD()
```lua copy
Core.HideHUD()
```

### Core.ShowHUD()
```lua copy
Core.ShowHUD()
```

### Core.BlurScreen(...args)
bool, ms
```lua copy
Core.BlurScreen()
```

### Core.ShowNotification(...args)
message, title, type, duration
```lua copy
Core.ShowNotification('Hello World!', 'Opod is cool', 'info', 5000)
```

### Core.GetNearestVehicle()
```lua copy
local vehicle = Core.GetNearestVehicle()
```

### Core.GetClosestPlayer()
```lua copy
local player = Core.GetClosestPlayer()
```

### Core.IsPlayerInAnyVehicle()
```lua copy
local inVehicle = Core.IsPlayerInAnyVehicle()
```

### Core.CloseInteraction()
```lua copy
Core.CloseInteraction()
```

### Core.OpenActionMenu(...args)
onClick, onCancel
```lua copy
local onClick = function()

end

local onCancel = function()

end

Core.OpenActionMenu(onClick, onCancel)
```

### Core.AddInteraction(...args)
key, text, hold, duration
```lua copy
Core.AddInteraction('E', 'Refill Gas', 5000)
```

### Core.RemoveInteraction(key)
```lua copy
Core.RemoveInteraction('E')
```

### Core.ScreenFadeOut(ms)
```lua copy
Core.ScreenFadeOut(2500)
```

### Core.ScreenFadeIn(ms)
```lua copy
Core.ScreenFadeIn(2500)
```

### Core.ProgressBar(table)
```lua copy
local properties = {
    onFinish = function(canceled)
        if not canceled then return end

        print('finished)
    end,
    duration = 5000,
    label = "Fishing",
}
Core.ProgressBar(properties)
```